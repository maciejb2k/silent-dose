<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="#"><%= link_to 'Raporty', daily_reports_path %></a></li>
    <li class="breadcrumb-item active" aria-current="page"><%= @daily_report.report_date %></li>
  </ol>
</nav>

<%= render partial: 'hero', locals: { daily_report: @daily_report } %>

<%= render partial: 'progress_bar', locals: { taken_medications: @taken_medications, total_medications: @total_medications } %>

<div class="d-flex flex-column flex-md-row align-items-center justify-content-center">
  <div class="list-group w-100">
    <%= render partial: 'daily_reports/medications/medication', collection: @medications %>
  </div>
</div>
